;((undefined) => {

  Dunno.UI.RegionView =
    class extends Dunno.UI.BaseView {
      constructor(options = {}) {
        super(options)
      }
  }

  window.customElements.define('region-view', Dunno.UI.RegionView)
})()
